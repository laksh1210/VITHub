/**
 * Gemini-powered AI chat module (Phase 4.4): a single-turn
 * {@code POST /api/ai/chat} endpoint backed by Google's Gemini API.
 * Scoped intentionally narrow — no conversation memory, history,
 * streaming, RAG, file uploads, image generation, function calling or
 * analytics belong here; those are later phases.
 */
package com.vithub.backend.ai;
