/**
 * JPA entities mapped to the database schema.
 * User/Role added in Phase 2 (authentication); remaining domain
 * entities (buildings, rooms, occupancy, etc.) are added from Phase 3 onward.
 */
package com.vithub.backend.entity;
