import { NextResponse } from "next/server";

export function middleware() {
  // Placeholder for future middleware logic
  return NextResponse.next();
}

export const config = {
  matcher: [], // Add matchers here later
};
